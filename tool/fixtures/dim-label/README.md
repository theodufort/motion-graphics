# dim-label fixture
Fixture: a label that peaks at a=0.3 at the 0.4 parked frame of the loop and
sits at a=0.05 for the other 3 parked frames. It passes the always-invisible error
(peak >= 0.15) but must trigger the new "dim label" warning
(below a=0.15 for >=75% of frames).
