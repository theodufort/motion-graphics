# seam-pop-out fixture
Fixture: a label fully visible just before the loop wrap but absent just
after it (alpha 1 at t=LOOP-300, 0 at t=300). The new seam pop-out
warning must fire; the pop-IN check must NOT (the label never appears
during the wrap window).
