# nearly-frozen fixture
Fixture: a large static panel with the ONLY animation being a
single-digit counter (9→0) in the center. The churn from one digit
changing is below the 2% near-frozen threshold — this fixture should
trigger the "near-frozen" warning (subtle motion) but NOT the "frozen"
error (static canvas).
