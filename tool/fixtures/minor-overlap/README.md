# minor-overlap fixture
Fixture: two labels overlapping by ~10px horizontally (a hairline touch,
not a full collision). Acceptance: a "minor label overlap" warning
fires (not an error), and the warning includes the pixel overlap area.
